{
  "name": "qa-cicd-codespace-demo",
  "version": "1.0.0",
  "description": "CI/CD demo project for QA session",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
